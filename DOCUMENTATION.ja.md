# Yuki System — 完全ドキュメント

本ドキュメントは、Yuki エコシステム全体、すなわちアーキテクチャ、各モジュールの役割、通信プロトコル、セキュリティモデル、そしてデプロイをエンドツーエンドで構築する方法をカバーします。概要のみが必要な場合は [README.md](README.md) を参照してください。`yuki-speaker` は独立した、より古い ESP32 モジュールであり、本ドキュメントおよびエコシステムの 1.0.0 強化作業の対象には含まれていません。

## 1. アーキテクチャ

```text
                          ┌──────────────┐
                          │  yuki-core   │  Python / asyncio WebSocket サーバー
                          │ (「頭脳」)   │  ws://host:8000  (/device, /webui)
                          └──────┬───────┘
                 ┌───────────────┼────────────────────────┐
                 │               │                         │
          ┌──────▼─────┐  ┌──────▼──────┐          ┌───────▼────────┐
          │ yuki-webui │  │  デバイス    │          │  yuki-protocol │
          │ Flask +    │  │  (/device)   │          │  言語ごとの    │
          │ ブラウザJS │  │              │          │  共有SDK       │
          │ (/webui)   │  └──────┬───────┘          │                │
          └────────────┘         │                  └────────────────┘
                 ┌────────────────┼─────────────────────┬───────────────┐
                 │                │                     │               │
        ┌────────▼───────┐ ┌──────▼───────┐  ┌──────────▼────────┐ ┌────▼─────────────┐
        │ yuki-device-pc │ │ yuki-device- │  │ yuki-device-pc-   │ │ yuki-humidifier   │
        │ (Windows, C#)  │ │ android      │  │ linux (Python/    │ │ (ESP32-C3, C++)   │
        │                │ │ (Kotlin)     │  │ GTK4/libadwaita)  │ │                   │
        └────────────────┘ └──────────────┘  └────────────────────┘ └───────────────────┘
```

`yuki-core` は、他のすべてのモジュールが直接やり取りする唯一のコンポーネントです。デバイスは `/device` に接続し、共有のベアラートークンで認証した上で、ステータス/コマンド/メトリクスをやり取りします。ブラウザは `/webui`（`yuki-webui` の Flask アプリが配信する静的ダッシュボード）に接続し、自身のトークンで認証した後、デバイスと同じリアルタイムビューに加えて管理操作（デバイスの承認、トークンのローテーション、監査ログの閲覧など）を利用できます。

## 2. モジュール

| モジュール | 言語 | 役割 |
|---|---|---|
| [`yuki-core`](../yuki-core) | Python | サーバー: ルーティング、デバイスの認証/認可、メトリクス/監査ログの保存、トークンローテーション |
| [`yuki-protocol`](../yuki-protocol) | Python / C# / JavaScript / C++ | 共有メッセージ形式、言語ごとに実装 |
| [`yuki-webui`](../yuki-webui) | Python（Flask）+ JS | Web ダッシュボード: デバイス一覧、グループ、タグ、ウィジェット、設定 |
| [`yuki-device-pc`](../yuki-device-pc) | C#（WinForms） | Windows リモート制御クライアント |
| [`yuki-device-pc-linux`](../yuki-device-pc-linux) | Python（GTK4/libadwaita） | Linux リモート制御クライアント、Windows 版と同じ機能セット |
| [`yuki-device-android`](../yuki-device-android) | Kotlin | Android リモート制御クライアント |
| [`yuki-humidifier`](../yuki-humidifier) | C++（Arduino/ESP32） | スマート加湿器ファームウェア |

## 3. プロトコル

すべての WebSocket 通信は JSON で、1フレームにつき1メッセージ、バイナリフレーミングはありません。

```json
{"protocol": "yuki/1.0", "type": "...", "id": "...", "timestamp": 0, "payload": {}}
```

`type` はメッセージの意味を選択します（`hello`、`welcome`、`status`、`command`、`command_result`、`metrics`、`device_to_device` など）。すべての実装は `protocol` が正確に `yuki/1.0` であることを検証し、それ以外を拒否します - バージョン間の互換性を吸収する仕組みは存在しないため、1つのデプロイではどこでも同じプロトコルバージョンを使用する必要があります。メッセージの完全な一覧は [`yuki-protocol`](../yuki-protocol) 自身の README を参照してください。

## 4. セキュリティモデル

- **デバイスの認証**: 共有のベアラートークン（`auth_token`。`yuki-core` が生成し、帯域外で各デバイスに配布します - `.token` の内容をコピーするか、自分で `YUKI_AUTH_TOKEN` を設定してください）。比較には定数時間比較を使用します。
- **デバイスの認可**: 有効なトークンを提示するだけでは不十分です - `yuki-core` にとって未知のデバイスは、管理者が `yuki-webui` から承認するまで保留状態に置かれます（あるいは、あらかじめ認可済みデバイス群に登録されている場合を除きます）。デバイスを完全にブラックリストへ登録することもできます。
- **WebUI のログイン**: `yuki-webui` はデフォルトで `admin`/`admin` のログイン情報を持っています（ディスク上ではハッシュ化されており平文ではありません）- ログイン後はダッシュボードの設定パネルから変更してください。ブラウザから `yuki-core` へのリアルタイム `/webui` ソケットには独自のトークンハンドシェイクが必要です（このトークンはログイン後にのみ `yuki-webui` から取得できます）。そのため、未認証のブラウザが制御プレーンのソケットに到達することはできません。
- **コマンド許可リスト**: すべてのクライアント（PC、Linux、Android、加湿器）は、自分が実行してもよいコマンドを通知し、そのリストにないコマンドは `yuki-core` が何を許可しているかに関わらず*ローカルで*拒否します - これにより、侵害されたサーバーや設定ミスのあるサーバーであっても、デバイスの所有者が明示的に無効化したコマンドを実行させることはできません。
- **通信の暗号化**: 典型的な、信頼された LAN 上でのホームオートメーションのデプロイに合わせて、**どこでもデフォルトではオフ**です（平文の `ws://`/`http://`）。すべてのコンポーネントで TLS を任意に有効化できます - `yuki-core`（`YUKI_TLS_ENABLED`/`YUKI_TLS_CERT`/`YUKI_TLS_KEY`）、`yuki-webui`（`YUKI_WEBUI_TLS_ENABLED` など）、`yuki-humidifier`（`Config.h` 内の `ENABLE_TLS`）、そしてデスクトップ/モバイルクライアントでは単に `wss://`/`https://` のアドレスを使うだけです - 正確な変数名は各モジュール自身の README を参照してください。
- **保存データの秘密情報**: core のトークンと webui の認証情報は、制限的なファイルパーミッション（600）で保存され、該当する場合はハッシュ化されます（webui のパスワード）。Windows クライアントは、保存するトークンを DPAPI で暗号化します。

これらはいずれも、実際に信頼できるネットワーク（自宅の LAN、VPN など）でエコシステムを運用することの代わりにはなりません - 同じネットワーク上のデバイスやブラウザが互いに対して何をできるかのハードルを上げるものであり、`yuki-core` をインターネットに直接公開しても安全なものに変えるわけではありません。

## 5. デプロイのセットアップ

1. `yuki-core` を起動します（README を参照）- `.token` に生成されたトークンを控えるか、初回起動前に自分で `YUKI_AUTH_TOKEN` を設定してください。
2. `yuki-webui` を起動し、`admin`/`admin` でログインした後、設定からパスワードを変更してください。
3. 各デバイス（加湿器は自身の AP セットアップページから、PC/Linux/Android クライアントは各自の UI から）に、`yuki-core` のアドレスと手順1のトークンを設定してください。
4. 新しいデバイスは `yuki-webui` 上に保留状態として表示されます - そこで承認してください。
5. 他の部分が問題なく動作するようになったら、任意で各モジュールの README に従ってすべての箇所で TLS を有効にしてください。

## 6. ロードマップ

プロジェクトの現在の状況とロードマップについては [README.md](README.md) を参照してください。本ドキュメントは、すべてのモジュールにわたって強化・統合された 1.0.0 のベースラインを記述しています。
